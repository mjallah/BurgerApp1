<li>
    {{this.burger_name}}

    <button class="eat btn-primary" data-id="{{this.id}}" data-devoured="{{this.devoured}}">
        Devour!
        {{#if devoured}}<i class="fas fa-utensils"></i>{{/if}} </button>
</li>